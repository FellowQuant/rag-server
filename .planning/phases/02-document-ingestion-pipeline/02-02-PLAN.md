---
phase: 02-document-ingestion-pipeline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rag_server/ingestion/embedder.py
autonomous: true
requirements:
  - INGEST-05

must_haves:
  truths:
    - "BGE-M3 model loads once via BGEM3FlagModel('BAAI/bge-m3', use_fp16=True)"
    - "embed_chunks() accepts list[ParsedChunk] and returns list[EmbeddingResult] with dense_vector (1024d) and sparse_indices/sparse_values"
    - "Dense vectors are 1024-dimensional float lists matching QdrantStore's DENSE_DIM=1024"
    - "Sparse vector indices are raw int token IDs from lexical_weights (NOT converted to strings)"
    - "Batch size defaults to 8 and is configurable to handle VRAM constraints"
    - "Empty text strings return zero dense vector and empty sparse indices rather than crashing"
  artifacts:
    - path: "src/rag_server/ingestion/embedder.py"
      provides: "BGE-M3 wrapper: Embedder class with load() and embed_chunks()"
      exports: ["Embedder", "EmbeddingResult"]
  key_links:
    - from: "src/rag_server/ingestion/embedder.py"
      to: "FlagEmbedding.BGEM3FlagModel"
      via: "model.encode(return_dense=True, return_sparse=True)"
      pattern: "BGEM3FlagModel"
    - from: "src/rag_server/ingestion/embedder.py"
      to: "src/rag_server/ingestion/chunker.py"
      via: "accepts list[ParsedChunk], reads chunk.content"
      pattern: "from rag_server.ingestion.chunker import ParsedChunk"
---

<objective>
Build the BGE-M3 embedding module that converts ParsedChunk content into dense + sparse vectors ready for Qdrant upsert.

Purpose: The worker pipeline calls the embedder after parsing. The embedder is stateful (holds the loaded model) and is instantiated once at worker startup to avoid repeated ~5-10s model load times.
Output: src/rag_server/ingestion/embedder.py with Embedder class and EmbeddingResult dataclass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-document-ingestion-pipeline/02-RESEARCH.md
@src/rag_server/ingestion/chunker.py
@src/rag_server/vector_store/qdrant.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: BGE-M3 embedder module</name>
  <files>
    src/rag_server/ingestion/embedder.py
  </files>
  <action>
**src/rag_server/ingestion/embedder.py** — create with this exact content:

```python
"""BGE-M3 embedding wrapper for the ingestion pipeline.

Wraps BGEM3FlagModel (FlagEmbedding library) to produce dense (1024d) and
sparse (lexical weight) vectors from ParsedChunk content strings.

Key design decisions:
- Embedder is instantiated ONCE at worker startup and reused across all jobs.
  Do not instantiate inside per-document pipeline functions.
- lexical_weights keys are raw int token IDs from the XLM-RoBERTa tokenizer.
  They are used DIRECTLY as Qdrant SparseVector indices — do NOT call
  convert_id_to_token() which returns string keys (Qdrant needs ints).
- Default batch_size=8 balances throughput vs VRAM. Reduce to 4 if OOM.
- Empty content strings return a zero dense vector and empty sparse vectors
  rather than crashing, so the pipeline can still store the chunk.
"""
from __future__ import annotations

import logging
from dataclasses import dataclass, field

import numpy as np
from FlagEmbedding import BGEM3FlagModel

from rag_server.ingestion.chunker import ParsedChunk

logger = logging.getLogger(__name__)

DENSE_DIM = 1024          # BGE-M3 dense output dimensionality
DEFAULT_BATCH_SIZE = 8    # Reduce to 4 if CUDA OOM during embedding
MAX_LENGTH = 512          # Token limit per chunk (matches chunker.MAX_TOKENS)


@dataclass
class EmbeddingResult:
    """Dense + sparse vectors for a single chunk, ready for Qdrant upsert."""
    chunk_index: int
    dense_vector: list[float]       # 1024-dimensional float list
    sparse_indices: list[int]       # BGE-M3 lexical weight token IDs (raw ints)
    sparse_values: list[float]      # Corresponding lexical weight floats


class Embedder:
    """Stateful BGE-M3 embedder. Instantiate once per worker process.

    Usage:
        embedder = Embedder()
        embedder.load()                    # downloads/loads model (~1 GB VRAM)
        results = embedder.embed_chunks(chunks)
        # later, on shutdown:
        embedder.unload()                  # frees VRAM
    """

    def __init__(self, batch_size: int = DEFAULT_BATCH_SIZE) -> None:
        self._batch_size = batch_size
        self._model: BGEM3FlagModel | None = None

    def load(self) -> None:
        """Load BGE-M3 model into memory. Blocks until model is ready (~5-10s)."""
        logger.info("Embedder: loading BAAI/bge-m3 (use_fp16=True)...")
        self._model = BGEM3FlagModel("BAAI/bge-m3", use_fp16=True)
        logger.info("Embedder: model loaded, batch_size=%d", self._batch_size)

    def unload(self) -> None:
        """Delete model reference to allow CUDA VRAM reclamation."""
        if self._model is not None:
            del self._model
            self._model = None
            logger.info("Embedder: model unloaded")

    def embed_chunks(self, chunks: list[ParsedChunk]) -> list[EmbeddingResult]:
        """Embed a list of ParsedChunk objects.

        Processes chunks in batches. Empty-content chunks receive a zero dense
        vector and empty sparse vectors — they are still stored in Qdrant so
        the chunk_index sequence remains intact for retrieval.

        Args:
            chunks: List of ParsedChunk from any parser.

        Returns:
            List of EmbeddingResult in the same order as input chunks.
        """
        if self._model is None:
            raise RuntimeError("Embedder.load() must be called before embed_chunks()")

        if not chunks:
            return []

        results: list[EmbeddingResult] = []

        # Process in batches to bound peak VRAM usage.
        for batch_start in range(0, len(chunks), self._batch_size):
            batch = chunks[batch_start : batch_start + self._batch_size]
            texts = [c.content for c in batch]

            # Separate empty texts to avoid model errors; they get zero vectors.
            non_empty_indices = [i for i, t in enumerate(texts) if t.strip()]
            non_empty_texts = [texts[i] for i in non_empty_indices]

            # Pre-populate with zero vectors for empty texts.
            batch_results: list[EmbeddingResult | None] = [None] * len(batch)
            for i, chunk in enumerate(batch):
                if i not in non_empty_indices:
                    batch_results[i] = EmbeddingResult(
                        chunk_index=chunk.chunk_index,
                        dense_vector=[0.0] * DENSE_DIM,
                        sparse_indices=[],
                        sparse_values=[],
                    )

            if non_empty_texts:
                output = self._model.encode(
                    non_empty_texts,
                    batch_size=self._batch_size,
                    max_length=MAX_LENGTH,
                    return_dense=True,
                    return_sparse=True,
                    return_colbert_vecs=False,  # not used in Phase 2; saves VRAM
                )
                dense_vecs: np.ndarray = output["dense_vecs"]        # shape (N, 1024)
                lexical_weights: list[dict[int, float]] = output["lexical_weights"]

                for result_pos, original_idx in enumerate(non_empty_indices):
                    raw: dict[int, float] = lexical_weights[result_pos]
                    # CRITICAL: raw keys are int token IDs — use directly as
                    # Qdrant sparse indices. Do NOT call convert_id_to_token().
                    batch_results[original_idx] = EmbeddingResult(
                        chunk_index=batch[original_idx].chunk_index,
                        dense_vector=dense_vecs[result_pos].tolist(),
                        sparse_indices=list(raw.keys()),
                        sparse_values=list(raw.values()),
                    )

            results.extend(r for r in batch_results if r is not None)

        logger.debug("Embedder: embedded %d chunks", len(results))
        return results
```
  </action>
  <verify>
Run (does not require GPU — tests structure only):
```bash
cd /home/jcanossa/workspace/fellow-quant/core/rag_server
python -c "
from rag_server.ingestion.embedder import Embedder, EmbeddingResult, DENSE_DIM
from rag_server.ingestion.chunker import ParsedChunk

# Verify module imports and class structure without loading model
e = Embedder(batch_size=4)
assert e._model is None
assert e._batch_size == 4

# Verify EmbeddingResult dataclass
r = EmbeddingResult(chunk_index=0, dense_vector=[0.0]*1024, sparse_indices=[1,2], sparse_values=[0.5, 0.3])
assert len(r.dense_vector) == DENSE_DIM
assert r.sparse_indices == [1, 2]
print('Embedder structure OK')

# Verify calling embed_chunks without load() raises RuntimeError
import traceback
try:
    e.embed_chunks([ParsedChunk(chunk_type='text', content='test')])
    assert False, 'should have raised'
except RuntimeError as ex:
    assert 'load()' in str(ex)
    print('RuntimeError guard OK')

print('All embedder checks passed')
"
```
  </verify>
  <done>Embedder class instantiates with configurable batch_size; EmbeddingResult dataclass has dense_vector (1024d), sparse_indices (list[int]), sparse_values (list[float]); calling embed_chunks() before load() raises RuntimeError; module imports without error after pip install.</done>
</task>

</tasks>

<verification>
Module structure verification (no GPU required):
```bash
cd /home/jcanossa/workspace/fellow-quant/core/rag_server
python -c "
import inspect
from rag_server.ingestion.embedder import Embedder, EmbeddingResult, DENSE_DIM
assert DENSE_DIM == 1024
assert hasattr(Embedder, 'load')
assert hasattr(Embedder, 'unload')
assert hasattr(Embedder, 'embed_chunks')
fields = {f.name for f in inspect.fields(EmbeddingResult) if hasattr(inspect, 'fields')}
print('Embedder module verified')
"
```
</verification>

<success_criteria>
- src/rag_server/ingestion/embedder.py exports Embedder and EmbeddingResult
- Embedder.load() sets self._model to BGEM3FlagModel instance
- Embedder.embed_chunks() processes in batches; empty strings produce zero vectors rather than errors
- EmbeddingResult.sparse_indices contains raw int token IDs (not strings)
- Dense vector dimension is 1024 matching QdrantStore.DENSE_DIM
- Module imports without error after pip install FlagEmbedding
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-ingestion-pipeline/02-02-SUMMARY.md` following the template at `.claude/get-shit-done/templates/summary.md`.
</output>
